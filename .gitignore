# Builds (PyInstaller)
app/dist/
packaging/dist/
packaging/staging_lite/
build/

# Python
__pycache__/
*.pyc
*.pyo

# Datos de entrada/salida (audio, archivos, modelo offline)
audio/
archivos/
modelo/

# Modelo TTS descargado (fuente de verdad del build; se descarga en runtime)
resource/

# Preferencias de la interfaz (generadas en runtime)
app/preferencias.json

# Entorno local
.atl/
.venv/
venv/
